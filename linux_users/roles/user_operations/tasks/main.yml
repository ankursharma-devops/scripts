---
name: Add users to the system
include: add_users.yml
when: action == "add_user"

name: Delete users from the system
include: del_users.yml
when: action == "del_user"

name: Disable users from the system
include: disable_users.yml
when: action == "disable_user"
